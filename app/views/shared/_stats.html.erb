<!--ми включаємо цей паршал в home і в profile
в home ми не має змінної @user а в profile вона вже є
тому ми пишем @user ||= current_user щоб на 
сторінці profile даром не викликати метод current_user-->
<% @user ||= current_user %> 
<div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <strong id="following" class="stat">
      <%= @user.followed_users.count %>
    </strong>  
    following
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong>
    followers
  </a>
</div>